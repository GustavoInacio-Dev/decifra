# API e cliente
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
requests>=2.28          # cliente HTTP e transcrição pelo Wit.ai

# Leitor de imagem (OCR local). Pesado: ~1.34 GB de pesos na 1a chamada.
# Instale o torch de CPU, que é muito menor:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40
torch>=2.0
pillow>=9.0

# Leitor de áudio
numpy>=1.24
SpeechRecognition>=3.10   # motor de reserva (Google SR)
imageio-ffmpeg>=0.4       # converte áudio para o Google SR
